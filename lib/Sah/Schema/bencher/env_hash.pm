package Sah::Schema::bencher::env_hash;

# AUTHORITY
# DATE
# DIST
# VERSION

our $schema = ['hash', {
    summary => 'A hash of environment variable and their value',
    of => ['str', {}],
}];

1;
# ABSTRACT:
